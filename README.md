## 🚀 기능 목록

1. 쉼표(,) 또는 콜론(:)으로 구분된 문자열에서 숫자를 추출하여 더하는 기능
2. 커스텀 구분자를 지원하는 기능 (예: "//;\n1;2;3"과 같은 형식에서 ;를 구분자로 사용)
3. 빈 문자열 또는 null 입력 시 0을 반환하는 기능
4. 음수를 입력하면 `IllegalArgumentException`을 발생시키는 기능
5. 잘못된 입력 형식에 대한 예외 처리 기능
